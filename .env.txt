# .env - Environment variables for Local AI Family Wellness Platform
# ---- IMPORTANT: DO NOT COMMIT THIS FILE TO VERSION CONTROL! ----
# ---- Add '.env' to your .gitignore file. ----
# ---- Replace examples below with strong, randomly generated secrets. ----

# --- PostgreSQL Database ---
POSTGRES_USER=postgres
POSTGRES_PASSWORD=YOUR_STRONG_POSTGRES_PASSWORD_HERE # Replace!
POSTGRES_DB=localai_family

# --- MinIO Object Storage ---
# Root credentials (used ONLY to start MinIO service itself)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=YOUR_STRONG_MINIO_ROOT_PASSWORD_HERE # Replace!

# Application-specific credentials (CREATE THESE MANUALLY IN MINIO CONSOLE AFTER FIRST RUN)
# These are used by the backend, celery worker, and tool servers
MINIO_APP_ACCESS_KEY=YOUR_MINIO_APP_ACCESS_KEY_HERE # Create in MinIO UI -> Access Keys
MINIO_APP_SECRET_KEY=YOUR_MINIO_APP_SECRET_KEY_HERE # Create in MinIO UI -> Access Keys

# --- Keycloak Authentication ---
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=YOUR_STRONG_KEYCLOAK_ADMIN_PASSWORD_HERE # Replace!

# --- Redis Cache & Broker ---
REDIS_PASSWORD=YOUR_STRONG_REDIS_PASSWORD_HERE # Replace!

# --- Grafana Monitoring ---
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=YOUR_STRONG_GRAFANA_ADMIN_PASSWORD_HERE # Replace!

# --- Backend Specific (Optional - Add if needed by your app.core.config) ---
# Example: BACKEND_SECRET_KEY=YOUR_VERY_STRONG_RANDOM_SECRET_KEY_FOR_JWT_OR_SESSIONS # Replace!
# Example: API_V1_STR=/api/v1

# --- SMTP Relay for Email (if communication-tool uses it) ---
# Example: SMTP_HOST=smtp.example.com
# Example: SMTP_PORT=587
# Example: SMTP_USER=your_email_user
# Example: SMTP_PASSWORD=your_email_or_app_password
# Example: SMTP_SENDER_EMAIL=noreply@yourdomain.local

# --- Other potential secrets ---
# Example: NTFY_AUTH_TOKEN=YOUR_NTFY_PUBLISH_TOKEN (if configured in server.yml)